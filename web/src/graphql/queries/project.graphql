query Project($id: Int!) {
  project(id: $id) {
    id
    createdAt
    updatedAt
    name
    desc
    points
    skills {
      id
      type
    }
    categories {
      id
      name
    }
    creator {
      id
      username
    }
  }
}
